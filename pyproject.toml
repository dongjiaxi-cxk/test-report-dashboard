[project]
name = "test-report-dashboard"
version = "0.2.0"
description = "Web dashboard for managing and visualizing API test reports"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "jinja2",
    "python-multipart",
]